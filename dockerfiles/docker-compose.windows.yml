version: "3.9"

services:
  ros2-dev:
    build:
      context: ..
      dockerfile: dockerfiles/Dockerfile.windows
    container_name: ros2_dev_container
    volumes:
      - ../:/app:cached
    tty: true
